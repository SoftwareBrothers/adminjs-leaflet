import { LatLngExpression } from 'leaflet';

// eslint-disable-next-line max-len
export const defaultAttribution = '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors';

export const defaultTileUrl = 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png';

export const defaultZoom = 5;

export const defaultCenter: LatLngExpression = [51.509865, -0.118092]; // London
